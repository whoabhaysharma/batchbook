/**
 * This interface defines the structure of a subject enrollment 
 * stored within the 'subjects' array in the Student document.
 */
export interface StudentSubject {
  name: string;
  price: number;
  tuitionId: string;
}
